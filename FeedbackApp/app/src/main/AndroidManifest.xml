<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.feedbackapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.RegisterActivity"
            android:label="@string/app_register">
        </activity>

        <activity
            android:name=".ui.login.HomeActivity"
            android:label="@string/app_home">
        </activity>

        <activity
            android:name=".ui.login.FeedbackOverview"
            android:label="@string/app_feed_over">
        </activity>

        <activity
            android:name=".ui.login.FeedbackWrite"
            android:label="@string/app_feed_write">
        </activity>


    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>